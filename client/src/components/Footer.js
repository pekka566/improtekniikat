import React from 'react';
import { Navbar } from 'react-bootstrap';
import './Footer.css';

const Footer = (/*props*/) => {
  return <Navbar fixedBottom={true}>Test</Navbar>;
};

export default Footer;
